from ...hof_quiz_shared import Condition, Program, Task

TASK = Task(
    name='population_brackets',
    prompt='todo',
    test='todo',
    header='def population_brackets(people):',
    footer='',
    programs=[
        Program(
            cond=Condition.LoopFused,
            correct=True,
            program='''
            
            ''')
    ])
